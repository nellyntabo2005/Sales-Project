# notifications/migrations/0002_create_default_templates.py
from django.db import migrations

def create_default_templates(apps, schema_editor):
    """Create default notification channels and templates"""
    
    NotificationChannel = apps.get_model('notifications', 'NotificationChannel')
    NotificationTemplate = apps.get_model('notifications', 'NotificationTemplate')
    
    # ============================================================
    # 1. Create Default Email Channel
    # ============================================================
    email_channel, email_created = NotificationChannel.objects.get_or_create(
        name='Email Channel',
        defaults={
            'channel_type': 'email',
            'config': {
                'from_email': 'noreply@yourstore.com',
                'reply_to': 'support@yourstore.com'
            },
            'is_active': True,
            'is_default': True,
            'priority': 1
        }
    )
    
    if email_created:
        print(f"✓ Created email channel")
    
    # ============================================================
    # 2. Create SMS Channel (for future use)
    # ============================================================
    sms_channel, sms_created = NotificationChannel.objects.get_or_create(
        name='SMS Channel',
        defaults={
            'channel_type': 'sms',
            'config': {
                'provider': 'africastalking',
                'api_key': 'YOUR_API_KEY',
                'username': 'YOUR_USERNAME'
            },
            'is_active': True,
            'is_default': False,
            'priority': 2
        }
    )
    
    if sms_created:
        print(f"✓ Created SMS channel")
    
    # ============================================================
    # 3. Low Stock Alert Template
    # ============================================================
    low_stock_template, low_stock_created = NotificationTemplate.objects.get_or_create(
        category='low_stock',
        channel=email_channel,
        defaults={
            'name': 'Low Stock Alert',
            'subject_template': '⚠️ Low Stock Alert: {{ product_name }}',
            'body_template': '''
Dear Team,

This is to notify you that product "{{ product_name }}" is running low on stock.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PRODUCT DETAILS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Product Name: {{ product_name }}
SKU: {{ product_sku }}
Current Stock: {{ stock_quantity }}
Reorder Level: {{ reorder_level }}
Recommended Order: {{ reorder_quantity }} units

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACTION REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Please place an order with the supplier immediately to avoid stockouts.

Best regards,
ERP System
            ''',
            'html_template': '''
<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .alert-box { background: #fff3cd; border-left: 4px solid #ffc107; padding: 15px; margin: 20px 0; }
        .details { background: #f8f9fa; padding: 15px; border-radius: 5px; margin: 15px 0; }
        .label { font-weight: bold; color: #856404; }
        .action { background: #d4edda; padding: 15px; border-radius: 5px; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="alert-box">
        <h2>⚠️ Low Stock Alert</h2>
        <p>Product <strong>{{ product_name }}</strong> is running low on stock!</p>
    </div>
    
    <div class="details">
        <h3>Product Details</h3>
        <p><span class="label">Product Name:</span> {{ product_name }}</p>
        <p><span class="label">SKU:</span> {{ product_sku }}</p>
        <p><span class="label">Current Stock:</span> {{ stock_quantity }}</p>
        <p><span class="label">Reorder Level:</span> {{ reorder_level }}</p>
        <p><span class="label">Recommended Order:</span> {{ reorder_quantity }} units</p>
    </div>
    
    <div class="action">
        <h3>Action Required</h3>
        <p>Please place an order with the supplier immediately to avoid stockouts.</p>
    </div>
    
    <hr>
    <p style="color: #6c757d; font-size: 12px;">ERP System - Automatic Notification</p>
</body>
</html>
            ''',
            'available_variables': ['product_name', 'product_sku', 'stock_quantity', 'reorder_level', 'reorder_quantity'],
            'is_active': True
        }
    )
    
    if low_stock_created:
        print(f"✓ Created low stock alert template")
    
    # ============================================================
    # 4. High Value Sale Alert Template
    # ============================================================
    high_value_template, high_value_created = NotificationTemplate.objects.get_or_create(
        category='high_value_sale',
        channel=email_channel,
        defaults={
            'name': 'High Value Sale Alert',
            'subject_template': '💰 High Value Sale: KES {{ sale_total }}',
            'body_template': '''
Congratulations! A high-value sale has been processed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SALE DETAILS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Sale ID: {{ sale_id }}
Amount: KES {{ sale_total }}
Customer: {{ customer_name }}
Cashier: {{ cashier_name }}
Items Purchased: {{ items_count }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NOTIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
This sale exceeds the configured threshold of KES 50,000.

Best regards,
ERP System
            ''',
            'html_template': '''
<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; }
        .success-box { background: #d4edda; border-left: 4px solid #28a745; padding: 15px; margin: 20px 0; }
        .details { background: #f8f9fa; padding: 15px; border-radius: 5px; }
        .amount { font-size: 28px; color: #28a745; font-weight: bold; }
    </style>
</head>
<body>
    <div class="success-box">
        <h2>💰 High Value Sale Alert</h2>
        <p>A high-value sale has been processed!</p>
    </div>
    
    <div class="details">
        <h3>Sale Details</h3>
        <p><strong>Sale ID:</strong> {{ sale_id }}</p>
        <p><strong>Amount:</strong> <span class="amount">KES {{ sale_total }}</span></p>
        <p><strong>Customer:</strong> {{ customer_name }}</p>
        <p><strong>Cashier:</strong> {{ cashier_name }}</p>
        <p><strong>Items Purchased:</strong> {{ items_count }}</p>
    </div>
    
    <hr>
    <p style="color: #6c757d; font-size: 12px;">ERP System - Automatic Notification</p>
</body>
</html>
            ''',
            'available_variables': ['sale_id', 'sale_total', 'customer_name', 'cashier_name', 'items_count'],
            'is_active': True
        }
    )
    
    if high_value_created:
        print(f"✓ Created high value sale alert template")
    
    # ============================================================
    # 5. Daily Sales Summary Template
    # ============================================================
    daily_summary_template, daily_created = NotificationTemplate.objects.get_or_create(
        category='daily_summary',
        channel=email_channel,
        defaults={
            'name': 'Daily Sales Summary',
            'subject_template': '📊 Daily Sales Summary - {{ date }}',
            'body_template': '''
Good morning,

Here is your daily sales summary for {{ date }}:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Total Sales: KES {{ total_sales }}
Transactions: {{ transaction_count }}
Average Transaction: KES {{ average_transaction }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏆 TOP 5 PRODUCTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{% for product in top_products %}
{{ forloop.counter }}. {{ product.product_name }} - {{ product.total_quantity }} units sold
{% empty %}
No sales recorded yesterday
{% endfor %}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ ALERTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Low Stock Items: {{ low_stock_count }}
Out of Stock Items: {{ out_of_stock_count }}

Have a great day!
ERP System
            ''',
            'html_template': '''
<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; }
        .header { background: #007bff; color: white; padding: 20px; text-align: center; }
        .summary { background: #f8f9fa; padding: 20px; margin: 20px 0; border-radius: 5px; }
        .amount { font-size: 32px; color: #28a745; font-weight: bold; }
        table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background: #007bff; color: white; }
        .alert { background: #fff3cd; padding: 15px; border-radius: 5px; margin: 20px 0; }
        .footer { text-align: center; color: #6c757d; font-size: 12px; margin-top: 30px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>📊 Daily Sales Summary</h1>
        <p>{{ date }}</p>
    </div>
    
    <div class="summary">
        <h2>Summary</h2>
        <p>Total Sales: <span class="amount">KES {{ total_sales }}</span></p>
        <p>Transactions: {{ transaction_count }}</p>
        <p>Average Transaction: KES {{ average_transaction }}</p>
    </div>
    
    <h2>🏆 Top 5 Products</h2>
    <table>
        <thead>
            <tr><th>Rank</th><th>Product</th><th>Units Sold</th></tr>
        </thead>
        <tbody>
            {% for product in top_products %}
            <tr>
                <td>{{ forloop.counter }}</td>
                <td>{{ product.product_name }}</td>
                <td>{{ product.total_quantity }}</td>
            </tr>
            {% empty %}
            <tr><td colspan="3">No sales recorded yesterday</td></tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="alert">
        <h3>⚠️ Alerts</h3>
        <p>Low Stock Items: {{ low_stock_count }}</p>
        <p>Out of Stock Items: {{ out_of_stock_count }}</p>
    </div>
    
    <div class="footer">
        <p>ERP System - Automatic Daily Report</p>
    </div>
</body>
</html>
            ''',
            'available_variables': [
                'date', 'total_sales', 'transaction_count', 'average_transaction',
                'top_products', 'low_stock_count', 'out_of_stock_count'
            ],
            'is_active': True
        }
    )
    
    if daily_created:
        print(f"✓ Created daily summary template")
    
    # ============================================================
    # 6. Return Request Template
    # ============================================================
    return_template, return_created = NotificationTemplate.objects.get_or_create(
        category='return_requested',
        channel=email_channel,
        defaults={
            'name': 'Return Request Notification',
            'subject_template': '🔄 Return Request - {{ return_number }}',
            'body_template': '''
A customer has requested a return.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RETURN DETAILS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Return Number: {{ return_number }}
Original Sale: {{ sale_id }}
Customer: {{ customer_name }}
Reason: {{ reason }}
Refund Amount: KES {{ refund_amount }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACTION REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Please review and approve/reject this return request.

Best regards,
ERP System
            ''',
            'available_variables': ['return_number', 'sale_id', 'customer_name', 'reason', 'refund_amount'],
            'is_active': True
        }
    )
    
    if return_created:
        print(f"✓ Created return request template")
    
    # ============================================================
    # 7. Customer Welcome Template (SMS)
    # ============================================================
    welcome_template, welcome_created = NotificationTemplate.objects.get_or_create(
        category='customer_welcome',
        channel=sms_channel,
        defaults={
            'name': 'Customer Welcome SMS',
            'subject_template': 'Welcome to {{ store_name }}!',
            'body_template': '''
Welcome {{ customer_name }}! Thank you for joining us. You now have {{ loyalty_points }} loyalty points. Visit us again!
            ''',
            'available_variables': ['customer_name', 'loyalty_points', 'store_name'],
            'is_active': True
        }
    )
    
    if welcome_created:
        print(f"✓ Created customer welcome SMS template")
    
    # ============================================================
    # 8. System Alert Template (Critical)
    # ============================================================
    system_alert_template, alert_created = NotificationTemplate.objects.get_or_create(
        category='system_alert',
        channel=email_channel,
        defaults={
            'name': 'System Alert',
            'subject_template': '🚨 SYSTEM ALERT: {{ alert_type }}',
            'body_template': '''
SYSTEM ALERT

Type: {{ alert_type }}
Time: {{ timestamp }}
Message: {{ message }}

Please investigate immediately.

ERP System
            ''',
            'available_variables': ['alert_type', 'timestamp', 'message'],
            'is_active': True
        }
    )
    
    if alert_created:
        print(f"✓ Created system alert template")
    
    print("\n✅ Default notification templates created successfully!")

def reverse_migration(apps, schema_editor):
    """Reverse migration - delete default templates"""
    NotificationTemplate = apps.get_model('notifications', 'NotificationTemplate')
    NotificationTemplate.objects.all().delete()
    print("✓ Removed all notification templates")

class Migration(migrations.Migration):
    dependencies = [
        ('notifications', '0001_initial'),  # Depends on initial migration
    ]
    
    operations = [
        migrations.RunPython(create_default_templates, reverse_migration),
    ]